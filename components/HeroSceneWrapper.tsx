/**
 * DEPRECATED — replaced by components/WebGLLaser.tsx in M3 v2.
 * Kept as an empty stub because the bash sandbox cannot unlink files.
 * Delete from PowerShell: Remove-Item components/HeroSceneWrapper.tsx
 */
export {};
